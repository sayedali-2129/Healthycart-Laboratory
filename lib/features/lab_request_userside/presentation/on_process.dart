import 'package:flutter/material.dart';

class OnProcess extends StatelessWidget {
  const OnProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('On Proces'),
    );
  }
}
